neme: integracion continua con node js

on: 
  pull_request:
    branches: [master]

jobs:
ci_to_master:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/ceckout@v2
    - use: actions/setup-node@v2
      node-version: 10
    - run: npm ci
    - run: npm test
    - run: npm run build